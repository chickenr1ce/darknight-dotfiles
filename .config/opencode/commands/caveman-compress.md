---
description: Compress the current session into a concise handoff summary
---
Compress this conversation into a concise handoff summary.

Include: the goal, key decisions, files touched, current state, next steps, open questions, and gotchas. Keep it tight — terse style, but names, paths, and technical terms exact. $ARGUMENTS
